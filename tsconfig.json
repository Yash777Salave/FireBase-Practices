{
  "extends": "@react-native/typescript-config",
  "compilerOptions": {
    "baseUrl": ".",              // <-- Add this
    "paths": {
      "*": ["src/*"]            // optional: if your project uses src/
    }
  },
  "include": ["**/*.ts", "**/*.tsx"],
  "exclude": ["**/node_modules", "**/Pods"]
}
